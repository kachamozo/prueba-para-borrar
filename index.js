const array = [1, 2, 3, 4, 5];

const multi = array.map((e) => e * 2);
console.log(multi);
console.log('hola mundo');

const persona = {
	nombre: 'Roly',
	apellido: 'Ochoa',
	edad: 35,
	estado: 'soltero',
};

console.log(persona.edad);
console.log(array);
